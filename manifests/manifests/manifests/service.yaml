apiVersion: v1
kind: Service
metadata:
  name: testgen
  namespace: default
  labels:
    app.kubernetes.io/name: testgen
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: testgen
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80